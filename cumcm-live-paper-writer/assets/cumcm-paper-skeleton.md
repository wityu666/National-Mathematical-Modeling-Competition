# 论文题目

> 本文件是原创赛时内容骨架，不包含或复刻任何本地 Word/LaTeX 模板样式。先按当届官方规则确定最终章节、首页和提交格式。

## 成稿控制

> **内部工作表：本表仅供成稿过程使用，不进入提交论文；导出前必须删除。**

| 字段 | 内容 |
|---|---|
| 题号 | `【填写】` |
| 主排版路线 | `【Word / LaTeX】` |
| 当届规则快照 | `【文件或链接】` |
| 结果冻结时间 | `【含时区】` |
| 冻结代码版本 | `【提交号或归档哈希】` |
| 冻结数据版本 | `【文件与哈希】` |
| 重复复核报告 | `【VER-*、PASS、model_freeze_id/run_id】` |
| 亮点账本 | `【路径、版本、FROZEN 状态】` |
| 当前论文版本 | `【版本号】` |
| paper_freeze_id | `【PAPER-###；Word/PDF 共用】` |
| Word 交付版 | `【.docx 路径、docx_sha256、可编辑性状态】` |
| PDF 冻结版 | `【.pdf 路径、pdf_sha256、pdf_generation_source】` |
| 双版本一致性 | `【PASS/BLOCKED；题目/摘要/关键词/章节/关键数值/证据标识/附录代码】` |
| 官方提交包选择 | `【仅 PDF / Word+PDF / 按当届规则填写；规则证据】` |
| 排版复核报告 | `【LAYOUT-*、PASS、paper_freeze_id、docx_sha256、pdf_sha256】` |
| 正文页数边界 | `【摘要起止 PDF 物理页 / 编号正文第一章起始 PDF 物理页 / 附录起始 PDF 物理页 / 编号正文页数（默认须为 26–30）】` |
| 页数门状态 | `【PASS / BLOCKED_PAGE_BOUNDARY / BLOCKED_PAGE_RANGE】` |
| PDF 总页数/附录页数 | `【总页数 / 附录不限页】` |
| 附录主要建模代码定位 | `【appendix_code_pdf_page / PASS / BLOCKED_APPENDIX_CODE】` |

> 编号正文默认须满足用户已确认的内部质量门 26–30 页：从第一章“问题重述”所在 PDF 物理页计至附录前一页；摘要、关键词和目录等正文前置部分不计入，附录页数不设上限。若当届官方上限低于 26 页，则 26 页下限自动失效并以官方上限为准。建议初稿正文控制在 27–28 页。不得通过缩小字号、压缩到不可读或把核心模型、结果、验证、符号表移入附录来规避上限，也不得通过放大图文、增大行距页边距、搬入代码/原始数据、重复叙述或添加无关图表来凑足下限。

> 最终本地交付必须同时保留同一冻结内容的可编辑 Word（`.docx`）与 PDF 两版。两版共享 `paper_freeze_id`，分别记录 SHA-256；不得分别修改。PDF 是冻结视觉版本，Word 是可编辑伴随版本。若当届官方提交规则只允许 PDF，Word 不进入官方提交包。

## 摘要

背景与目标：`【用 1–2 句说明研究对象、总任务和主线】`

贯穿主线与已证亮点：`【第一屏至少放一条 CONTRIB-PROVEN 的量化 proof，并绑定 RID/FIG/TAB】`

逐问摘要内容清单（每个小问均须全部交代）：

| 必含要素 | 本队内容 |
|---|---|
| 数据与关键处理 | `【数据范围、关键处理及必要口径】` |
| 模型或数学构造 | `【实际建立的模型、关系或数学构造】` |
| 求解方法 | `【实际使用的算法、求解器或推导路线】` |
| 带 RID 的关键结果 | `【RID-* 与对应数值、方案或规律】` |
| 带指标的验证方式 | `【验证方法、指标及实测结果】` |

> 上表只规定必须写什么，不规定句式。每问的开头语、句序、连接方式和五项内容的组织由本队依据实际论证自行决定；不得套用“针对问题 n，基于……建立……并采用……，得到……；通过……检验”一类统一句式，以免与其他队伍形成逐字相同的摘要骨架。

检验与结论：`【误差、敏感性、稳健性或推广边界】`

## 关键词

`【研究对象；模型；算法；关键指标】`

> 关键词使用研究对象、模型、算法和关键指标的具体名称，不写“数学建模”等空词。

## 目录

`【使用 Word 自动目录或 LaTeX \tableofcontents，根据正文标题样式自动生成】`

> 关键词紧随摘要，目录放在关键词之后、正文之前。目录标题本身不进入目录条目；目录至少覆盖正文各级有效标题、参考文献和附录，并与正文标题文字、层级和页码一致。不得手工输入或维护目录页码；正文标题、分页、图表或公式变化后，须更新全部域或完成足够轮次编译，再检查目录后导出最终 PDF。

## 一、问题重述

### 1.1 问题背景

`【用自己的话重述，不改变题意；需要引文时标 CIT】`

### 1.2 问题提出

| 小问 | 输入 | 要求输出 | 验收口径 | 实质答案状态 |
|---|---|---|---|---|
| Q1 | `【】` | `【】` | `【】` | `【COVERED/BLOCKED】` |
| Q2 | `【】` | `【】` | `【】` | `【COVERED/BLOCKED】` |
| Q3 | `【】` | `【】` | `【】` | `【COVERED/BLOCKED】` |

## 二、问题分析

### 2.1 问题一

- 要求输出与验收口径：`【】`
- 数学任务类型与核心难点：`【】`
- 数据特征与上问依赖：`【】`
- 方法选择理由：`【】`
- 预期检验与下问接口：`【】`

### 2.2 问题二

`【同上】`

### 2.3 问题三

`【同上】`

### 2.4 总体思路图

> 本图是全文的建模导航，不替代 2.1–2.3 的逐问分析、主体公式、数值结果或真实检验。图的结构必须由本题实际依赖决定：问间强依赖时可用单链式流程，分问相对独立时可用多面板，并存共用处理与分问分支时可用“共用主干＋分问分支”的混合结构；不得机械套用固定版式。

| 必含信息 | 本队填写 |
|---|---|
| 每问任务与输入 | `【Q1/Q2/Q3 的输入、数据或上问产物】` |
| 关键处理与假设 | `【实际使用的数据处理、关键假设或特征构造】` |
| 模型与求解 | `【实际采用的模型/数学构造、算法/求解器与必要判断】` |
| 检验与输出 | `【每问实际执行的验证，以及绑定 RID/FIG/TAB 的答案或交付】` |
| 问间依赖 | `【共享模块、传递变量/文件和依赖方向；相互独立时明确标注】` |
| 图源与冻结 | `【FIG-OVERVIEW-001；可编辑源、生成命令、paper/model/run freeze_id】` |

总体思路图：`【插入 FIG-OVERVIEW-001；图题按本题内容自拟，使用已冻结的 palette_set 与 object_color_map】`

> 不得照抄参考论文、示例图片或本套件说明中的框图布局、节点文字和连接方式，也不得保留水印、广告、机构标识或截图界面。单链、多面板或混合结构、节点层级和题注均由本队依据真实模型合同自行设计；图中不得出现正文未采用的方法，也不得用空泛“数据→模型→结果”框图或放大图尺寸来凑正文页数。

## 三、模型假设

> 本章集中列出三问共用的全局假设。各问建模过程中额外引入的本问专有假设，须在对应问题章节就近说明，并在本章建立可追溯索引，例如“问题二另有专有假设，见第七章”。不得把本问专有假设混入全局假设，也不得引入未说明的隐含假设。

| 编号 | 假设 | 依据 | 影响范围 | 违反时如何处理 |
|---|---|---|---|---|
| A1 | `【】` | `【数据/引文/常识】` | `【】` | `【】` |

## 四、符号说明

> 本节属于正文，固定放在“模型假设”之后、“数据收集与预处理/问题一的建立与求解”之前。不得把完整符号表移到附录。全文共用或反复出现的符号列在这里；仅在单个公式使用一次的局部符号可在公式附近定义。

> 下表在最终 Word/PDF 中必须排成三线表：只保留表格顶线、表头下横线和表格底线，不使用竖线、外侧左右边线、逐行横线或全边框网格。当前 Markdown 竖线仅用于表示内容列，不代表最终版式。跨页时重复表头，并保持续表的三线表语义与列对齐。

| 符号 | 含义 | 单位 | 取值范围/类型 | 首次出现位置 |
|---|---|---|---|---|
| `【】` | `【】` | `【】` | `【】` | `【节/公式】` |

符号使用规则：

- 同一符号全文只表达一个含义；同一含义不随意更换符号。
- 下标、上标、集合、向量/矩阵和随机变量的记法必须说明。
- 无量纲量在“单位”栏明确写“无量纲”，不要留空。
- 关键公式首次出现时，在相邻正文再次解释本式新增符号与单位。

## 五、数据收集与预处理

> 本章集中处理三问共用的数据工作。若问题二、问题三仍需大量各自专有的数据处理，应把该部分写入对应问题的建模章节，不要全部堆在本章；判断标准是行文逻辑是否连贯——共用数据处理集中写，问题专有数据处理就近写。无论写在哪里，数据来源、统计口径、`DATA-*` 标识与可追溯性要求都不得降低。

### 5.1 数据来源与版本

| 数据 | 来源 | 时间范围 | 文件/哈希 | 引文 |
|---|---|---|---|---|
| `【】` | `【】` | `【】` | `【】` | `CIT-###` |

### 5.2 质量检查

- 缺失值处理：`【缺失模式、处理方法、参数、理由及影响】`
- 数据清洗与标准化：`【重复、异常、单位、编码、时间对齐、清洗规则与标准化方法】`

### 5.3 预处理步骤

每一步说明输入、变换、参数、理由和输出文件。

数据分析：`【分布、相关关系、异常识别等探索性分析及其对后续建模的作用】`

> 三问必须覆盖同一套内容门，任一问缺项即视为漏答；但小节命名、组合与排列必须按各问自身的数学逻辑组织，不得三问雷同，也不得把本套件内容门机械转写成统一子结构。

> 主体三问的每一章都必须设置描述性小标题，用来呈现本问从定义、建模、求解、结果到验证的推进关系，不得整章写成无层次的连续长段。小标题的数量、名称、组合和顺序由本问数学逻辑决定；不得把“目标与接口”“数据与处理”“模型或数学构造”“求解与复现”“结果与解释”“真实检验”“直接答案”等内容门逐字复制为三问统一的小标题。

> 第三章只集中列全局假设；各问额外引入的专有假设必须在本问章节就近说明，并在第三章留有索引。不得把本问专有假设混入全局假设，也不得使用未说明的隐含假设。

> 主体公式按所在章自动编号并使用自动交叉引用，格式为“章号-序号”：第六章依次为式(6-1)、式(6-2)、式(6-3)，第七章依次为式(7-1)、式(7-2)。正文应写“由式(6-3)可得”，不得用“上式”“下式”指代，也不得手工输入编号。每章内须连续、无跳号、无重号；删除公式后必须重新编号并检查全部引用。

## 六、问题一的建立与求解

> 本章必须设置能概括实际推导、求解或验证任务的描述性小标题，不得整章写成无层次连续长段。下表是内容门禁，不是固定小节顺序；小节名、组合方式和排列由本问的数学逻辑决定，不得把内容门名称直接复制为小标题，不得在三问之间机械复用同一套子结构，也不得与其他队伍形成一致的固定子结构。表内每项均须在正文中有明确位置，缺项即为漏答。

| 必含内容 | 本队填写 |
|---|---|
| 目标、难点与上下问接口 | `【本问输出、核心难点、复用产物与后续接口】` |
| 本问专有假设 | `【本问额外引入的假设、引入理由、对结果的影响；无则写“无本问专有假设”】` |
| 数据与处理 | `【数据、处理动作、理由、输出文件及 DATA/RUN 标识】` |
| 模型或数学构造 | `【变量、参数、公式、目标、约束及适用条件】` |
| 求解与复现 | `【算法/求解器、版本、参数、seed、停止条件、入口命令与输出】` |
| 结果与解释 | `【RID/FIG/TAB → 比较 → 机制或实际含义 → 适用边界】` |
| 真实检验 | `【代回/逐约束复核、独立路线、误差指标、基线、消融、敏感性、极端情景或多 seed】` |
| 直接答案与下问接口 | `【绑定 RID 的题面答案；下一问复用的变量、文件或模型部件】` |

可选洞察/发现登记：

| contrib_id | 可证伪 claim | 量化 proof | evidence_ids | cost_risk / 失效边界 |
|---|---|---|---|---|
| `CONTRIB-Q1-001` | `【】` | `【】` | `【RID/FIG/TAB/MODEL】` | `【】` |

> 无当前冻结的 `CONTRIB-PROVEN` 时删除洞察内容，不得保留空泛“创新点”。

## 七、问题二的建立与求解

> 本章必须用描述性小标题呈现问题二的建模与证据推进，不得整章堆成无层次长段。下表只规定本问必须覆盖的内容门，不是固定小节顺序；小节名、内容组合和排列须由问题二的数学逻辑与证据链决定，不得把内容门名称直接复制为小标题，不得在三问之间机械复用同一套子结构，也不得与其他队伍形成一致的固定子结构。每项都应能在正文中定位，缺项即为漏答。

| 必含内容 | 本队填写 |
|---|---|
| 目标、难点与上下问接口 | `【本问输出、核心难点、承接上问的复用产物与后续接口】` |
| 本问专有假设 | `【本问额外引入的假设、引入理由、对结果的影响；无则写“无本问专有假设”】` |
| 数据与处理 | `【数据、处理动作、理由、输出文件及 DATA/RUN 标识】` |
| 模型或数学构造 | `【变量、参数、公式、目标、约束及适用条件】` |
| 求解与复现 | `【算法/求解器、版本、参数、seed、停止条件、入口命令与输出】` |
| 结果与解释 | `【RID-Q2-*/FIG-Q2-*/TAB-Q2-* → 比较 → 机制或实际含义 → 适用边界】` |
| 真实检验 | `【代回/逐约束复核、独立路线、误差指标、基线、消融、敏感性、极端情景或多 seed】` |
| 直接答案与下问接口 | `【绑定 RID-Q2-* 的题面答案；下一问复用的变量、文件或模型部件】` |

可选洞察/发现登记：

| contrib_id | 可证伪 claim | 量化 proof | evidence_ids | cost_risk / 失效边界 |
|---|---|---|---|---|
| `CONTRIB-Q2-001` | `【】` | `【】` | `【RID/FIG/TAB/MODEL】` | `【】` |

> 无当前冻结的 `CONTRIB-PROVEN` 时删除洞察内容，不得保留空泛“创新点”。

## 八、问题三的建立与求解

> 本章必须设置描述性小标题，使最终模型、求解、结果、检验与交付关系清楚可见，不得整章写成无层次长段。下表是问题三的完整内容门，而不是固定小节顺序；小节名、组合方式和排列必须服从本问自身的数学逻辑，不得把内容门名称直接复制为小标题，不得在三问之间机械复用同一套子结构，也不得与其他队伍形成一致的固定子结构。表内每项都须在正文中有明确位置，任何缺项均视为漏答。

| 必含内容 | 本队填写 |
|---|---|
| 目标、难点与上下问接口 | `【本问输出、核心难点、承接上问的复用产物与最终交付关系】` |
| 本问专有假设 | `【本问额外引入的假设、引入理由、对结果的影响；无则写“无本问专有假设”】` |
| 数据与处理 | `【数据、处理动作、理由、输出文件及 DATA/RUN 标识】` |
| 模型或数学构造 | `【变量、参数、公式、目标、约束及适用条件】` |
| 求解与复现 | `【算法/求解器、版本、参数、seed、停止条件、入口命令与输出】` |
| 结果与解释 | `【RID-Q3-*/FIG-Q3-*/TAB-Q3-* → 比较 → 机制或实际含义 → 适用边界】` |
| 真实检验 | `【代回/逐约束复核、独立路线、误差指标、基线、消融、敏感性、极端情景或多 seed】` |
| 直接答案与最终交付 | `【绑定 RID-Q3-* 的题面答案；需交付到附录或支撑材料的产物与复现入口，若无额外交付则明确说明】` |

可选洞察/发现登记：

| contrib_id | 可证伪 claim | 量化 proof | evidence_ids | cost_risk / 失效边界 |
|---|---|---|---|---|
| `CONTRIB-Q3-001` | `【】` | `【】` | `【RID/FIG/TAB/MODEL】` | `【】` |

> 无当前冻结的 `CONTRIB-PROVEN` 时删除洞察内容，不得保留空泛“创新点”。

## 九、模型评估改进与推广

### 9.1 优点

> 优点只来自当前冻结亮点账本中的 `CONTRIB-PROVEN` 及其量化证据。

| CONTRIB-PROVEN | 已证明优点 | 量化证据 | 代价/边界 |
|---|---|---|---|
| `CONTRIB-Q?-###` | `【】` | `【RID/FIG/TAB】` | `【】` |

### 9.2 不足

`【说明假设、数据、算法或外推边界】`

可执行改进：`【从仍有价值的 DROPPED 候选说明改进对象、被放弃原因、预计作用和重新验证方法】`

> 改进方向来自仍有价值的 `DROPPED` 候选，不得把尚未证明的改进写成既有优点。

### 9.3 推广

`【仅使用 delta_type=更好泛化 且有跨场景/外推证据的 CONTRIB-PROVEN；否则明确“不主张推广”】`

## 参考文献

`【按当届规则和正文首次引用顺序生成；每条均有 CIT 标识】`

## AI 使用记录

> 本节是否计入正文章节编号，以及采用独立成节还是并入附录，按当届官方规则决定；本骨架默认不给本节分配正文章节序号。

| 标识 | 工具与版本 | 时间 | 用途 | 关键交互存档 | 采纳与人工修改 | 论文位置 |
|---|---|---|---|---|---|---|
| `AI-001` | `【】` | `【】` | `【】` | `【文件】` | `【】` | `【】` |

## 附录

> 提交论文的附录只保留关键建模代码和确有复核价值的补充结果。不得设置或撰写“完整程序与支撑材料索引”“支撑材料清单”或类似章节，也不得把完整程序文件名、内部目录、冻结清单与哈希、验证报告、事实追踪表、图形注册表或 AI 交互文件索引写入论文。上述工程信息只保存在团队内部的运行清单、交付清单和审计报告中；AI 使用披露仍按当届官方规则在前述“AI 使用记录”位置处理。

### A. 关键建模代码

> 附录必须收录实际生成最终模型、求解结果或关键验证的建模代码。优先保留变量与参数构造、目标函数与约束、核心求解/预测/仿真过程以及验证逻辑；导包、通用绘图、重复文件读写和未采用的候选算法不作为关键代码。每段代码只用自然语言注明所属小问、功能和必要的运行条件，不展示内部证据编号、冻结目录、哈希清单或支撑材料索引；完整工程另随当届规则允许的支撑材料提交。

> 当届规则允许彩色时，本附录全部代码统一使用 `code_theme=VS-CODE-LIGHT-MUTED`：白底、等宽、可复制，按关键字、函数/方法、类/装饰器、字符串、注释、数值/常量等语义角色设置多色字体；关键字同时加粗、注释同时斜体。不得逐段换主题、随机配色、使用深色编辑器背景或粘贴代码截图。该代码主题与论文图表的 `palette_set` 相互独立；官方要求黑白时记录 `MONOCHROME_OFFICIAL_OVERRIDE` 与规则证据。

#### A.1 问题一关键代码

`【以可复制文本粘贴问题一关键代码；用自然语言注明功能，不使用代码截图】`

#### A.2 问题二关键代码

`【以可复制文本粘贴问题二关键代码；用自然语言注明功能，不使用代码截图】`

#### A.3 问题三关键代码

`【以可复制文本粘贴问题三关键代码；用自然语言注明功能，不使用代码截图】`

> 若多个问题共享同一核心函数，只在首次出现处完整列出，其余问题用自然语言交叉说明，不得重复粘贴同一代码。代码须使用等宽字体、合理换行和可辨字号，不得缩小到不可读，也不得把截图当作代码。`CODE-*`、源文件位置、冻结版本及 `MODEL/RUN/RID/FIG/TAB` 映射仅保存在导出前删除的内部同步表和审计材料中。

### B. 补充结果

`【正文不宜展开但评审复核所需的证明、表格或结果】`

> 核心结论、关键验证和复现入口必须留在正文；附录只承载展开材料。

## 提交前同步表

> **内部工作表：本表仅供成稿过程使用，不进入提交论文；导出前必须删除。**

| 检查项 | 状态 | 证据/位置 |
|---|---|---|
| 摘要数值与正文一致 | `【】` | `【RID】` |
| 所有关键结果绑定同版本 `VER-* PASS` | `【】` | `【Round A/Round B/硬门】` |
| 图表均有正文引用 | `【】` | `【】` |
| 图型按真实分析任务选择，未为凑齐扇形图、水平条形图、折线图或竖向柱形图补造图片 | `【】` | `【图表登记；未使用类型可记 NOT_APPLICABLE】` |
| 符号说明表位于正文模型假设之后、建模章节之前 | `【】` | `【正文节号/页码】` |
| 符号说明采用三线表且无竖线/逐行横线 | `【】` | `【Word/PDF 页码与截图；顶线/表头下横线/底线】` |
| 符号表字段完整，且符号和单位与公式附近定义一致 | `【】` | `【符号表/公式编号】` |
| 引文可定位 | `【】` | `【CIT】` |
| AI 记录完整 | `【】` | `【AI】` |
| A/B/C 题型与任务一致，未套用其他组别模板 | `【】` | `【】` |
| 每问均有结果—解释—验证闭环 | `【】` | `【RID/TAB/FIG】` |
| 每问都有可提交的实质答案，无空白小问 | `【】` | `【各问结论】` |
| 总体思路图覆盖每问真实链路并与正文一致 | `【】` | `【FIG-OVERVIEW-001；问题分析末尾/可编辑源/生成命令/freeze_id/Word-PDF QA】` |
| 附录已收录关键建模代码并与冻结运行、正文结果逐项映射 | `【】` | `【CODE/MODEL/RUN/RID/FIG/TAB；源文件/函数/版本】` |
| 附录主要建模代码已定位到最终 PDF 物理页且不是辅助代码冒充 | `【】` | `【appendix_code_pdf_page；PASS/BLOCKED_APPENDIX_CODE】` |
| 附录代码主题统一且 Word/PDF 多色语法高亮、灰度与复制抽查通过 | `【】` | `【code_theme=VS-CODE-LIGHT-MUTED / 官方黑白例外；语义颜色/加粗/斜体/纯文本复制】` |
| 提交论文未包含“完整程序与支撑材料索引”、支撑材料清单或内部工程文件路径 | `【】` | `【Word/PDF 附录页码；内部清单另存位置】` |
| 摘要第一屏包含量化 `CONTRIB-PROVEN` 和全文主线 | `【】` | `【CONTRIB/RID/FIG/TAB】` |
| 正文亮点均来自当前冻结账本，且已披露代价与边界 | `【】` | `【CONTRIB】` |
| 广告、水印、联系方式和模板示例已清除 | `【】` | `【】` |
| 占位符已清除 | `【】` | `【】` |
| 当前 PDF 已通过独立重复排版复核 | `【】` | `【LAYOUT-*、PASS、PDF SHA-256、截图范围】` |
| Word 与 PDF 两版均已生成并绑定同一冻结稿 | `【】` | `【paper_freeze_id / docx_sha256 / pdf_sha256 / pdf_generation_source】` |
| Word 可编辑且与 PDF 关键内容逐项一致 | `【】` | `【题目/摘要/关键词/章节/关键数值/证据标识/图表公式编号/附录代码核对】` |
| 本地交付包与官方提交包已按当届规则分离 | `【】` | `【Word 是否允许提交 / 官方规则证据 / 实际包清单】` |
| 编号正文实测满足 26–30 页内部质量门，摘要不计、附录页数不限 | `【】` | `【摘要起止物理页/编号正文第一章起始物理页/附录起始物理页/编号正文页数/总页数；官方上限低于 26 页时记录例外】` |
| 页数硬门已实际触发且边界完整 | `【】` | `【PASS/BLOCKED_PAGE_BOUNDARY/BLOCKED_PAGE_RANGE；layout_preflight 输出】` |
