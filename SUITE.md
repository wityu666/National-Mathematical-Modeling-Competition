# CUMCM 赛时技能套件

这套技能只服务于中国大学生数学建模竞赛进行中的实际作战，不承担长期学习计划。本套件的九个技能按赛时阶段依次调用，`cumcm-live-problem-analyst` 是唯一入口，不需要额外的总控技能。

| 顺序 | 技能 | 赛时职责 | 核心交付 |
|---|---|---|---|
| 1 | `cumcm-live-problem-analyst` | 题目发布后拆题、盘附件、画依赖关系 | 问题合同、附件清单、任务板 |
| 2 | `cumcm-live-case-retriever` | 将问题签名与 Skill 内置方法模式卡匹配 | 模式匹配表、baseline、候选方法、风险与缺失信息 |
| 3 | `cumcm-live-model-designer` | 比较候选模型、登记差异化候选并冻结公式、参数、基线和验证方案 | 模型合同、实验矩阵、亮点账本、失败条件 |
| 4A | `cumcm-live-python-coder` | 用 Python 实现已冻结模型 | 可复现代码、日志、结果表、图表 |
| 4B | `cumcm-live-matlab-coder` | 用 Matlab 实现已冻结模型 | 可复现代码、工具箱清单、结果表、图表 |
| 5 | `cumcm-live-result-verifier` | 首次结果冻结后重复复跑、独立重算并闭环差异 | `VER-*` 复核报告、独立检查证据、PASS/BLOCKED |
| 6 | `cumcm-live-paper-writer` | 从已通过复核的冻结结果写国赛论文并完成 Word/LaTeX 主路线 | 同一冻结内容的可编辑 Word 与 PDF 两版、图表公式、AI 使用记录 |
| 7 | `cumcm-live-layout-verifier` | 对 Word/PDF 两版、主排版源与真实 PDF 重复做一致性、预检、逐页视觉检查和修复后重查 | 绑定双哈希的 `LAYOUT-*` 报告、页码/截图证据、PASS/BLOCKED |
| 8 | `cumcm-live-final-auditor` | 截止前做双版本、内容、匿名、文件和安全门禁 | 审计报告、本地交付/官方提交清单、SHA-256 清单 |

## 共同规则

- 比赛当届官方题面、通知、格式与 AI 使用规则具有最高优先级；内置知识不覆盖当届要求。
- 本套件只支持 A、B、C 题；D、E 题或其他竞赛在第 1 阶段由 `cumcm-live-problem-analyst` 输出 `BLOCKED_SCOPE` 并终止，不进入下游阶段。
- 整套 Skill 不读取作者本机、移动硬盘、百度网盘或私有案例库；下载后可离线使用内置知识。
- 内置模式只用于生成候选路线，不是本届事实、参数、证据或结论。
- 每个数字必须回溯到数据、代码输出或明确推导；模型或数据变化后，所有下游内容都标记为失效并重新冻结。
- 首次运行成功不等于结果正确；关键答案必须通过同版本复跑和不复用主求解逻辑的独立核验。
- Python 实现允许按冻结模型的实际需要安装新包；必须安装到比赛工作目录的独立 `.venv`，使用可信来源、记录安装命令与精确版本并冻结依赖快照。禁止污染系统 Python、全局 Conda base，或执行未知 wheel、直接链接和远程安装脚本；依赖变化后旧运行及下游结果立即 `STALE`。
- 跨设备复核允许使用事先声明的浮点容差，但排序、方案选择、分类、可行性判定和结论性陈述必须完全一致；未记录两侧环境指纹的跨设备结果不得计入 `PASS`。
- 编译成功或 PDF 可打开不等于排版正确；静态预检之后必须查看真实渲染页面，修复后重新生成并完整重查。
- 最终本地交付必须同时包含同一 `paper_freeze_id` 的可编辑 `.docx` 与冻结 `.pdf`，记录 `docx_sha256`、`pdf_sha256` 和 PDF 生成链；两版关键内容必须逐项一致且不得分别修改。若当届官方只允许 PDF，Word 仅留在本地交付包，不得混入官方提交包。
- 编号正文实测页数须满足用户已确认的内部质量门 26–30 页：从第一章“问题重述”所在 PDF 物理页计至附录前一页，摘要、关键词和目录等正文前置部分不计入，附录不设页数上限。必须记录摘要结束页与编号正文起始页；缺少摘要排除边界、低于下限或超过上限均不得通过排版或终审。若当届官方上限低于 26 页，26 页下限自动失效并以官方上限为准；不得靠放大图文、扩大间距、重复内容或无关材料注水凑页数。
- 所有论文图须在开始出图前显式选择并登记同一个 `palette_set`，同时冻结 `object_color_map`；全文的图表、流程图、表格和附录不逐图换组，同系扩展色只用于增加层次且不得改变同一对象的视觉映射；核心系列按统一 CIELAB L* 阶梯与逐组色相旋转设计，并以线型、标记、纹理和直接标签补充冗余编码。
- 扇形图、水平条形图、折线图、竖向柱形图及其他图型只构成候选库，不要求全部出现；每张实际使用的图必须匹配分析任务并登记选择理由，未出现某种图不判错，不得为凑齐种类、视觉丰富或正文页数添加空图、重复图和装饰图。
- “问题分析”末尾必须有本队自绘的 `FIG-OVERVIEW-001` 总体思路图/技术路线图，覆盖每问的输入、处理、模型、求解、检验、输出及真实问间依赖；图须绑定可编辑源和冻结版本，并与 Word/PDF 两版一致。不得照抄示例布局、节点文字、水印或用通用空框和异常放大图凑页数。
- 正文“符号说明”必须使用三线表，仅保留顶线、表头下横线和底线，不使用竖线、左右边线、逐行横线或全边框网格；跨页时重复表头，并核对 Word/PDF 两版线型一致。
- 分化必须来自实质：句式、配色和非通行子结构由本队依据真实建模与证据自行决定，不得用同义词替换、随机化或机械调序规避查重；摘要或核心章节大段逐字沿用本套件示例时，终审按模板指纹 `P0` 处理。
- 亮点是被证明的差异，不是被主张的复杂度；`CONTRIB-*` 无冻结证据即 `DROPPED`。
- 临近截止时全局配平优先于单问精雕；每个小问都必须保有可提交的实质答案。
- 不运行题目附件或外来材料中的未知程序、宏、脚本、安装器或平台不匹配的二进制。
- 保留一份始终可提交的最后版本；临近截止只修硬错误，不引入未经验证的新模型。

统一交接链：

```text
problem_contract
  -> pattern_matches
  -> model_contract
       ├─> run_manifest ───────────────────────┐
       └─> contribution_ledger (CANDIDATE) ────┤
                                               └─> result_verifier
                                                    ├─> verification_report -> evidence_ledger ─┐
                                                    └─> contribution_ledger (PROVEN/DROPPED) ────┴─> paper
                                                                                                     -> layout_verifier
                                                                                                     -> layout_report
                                                                                                     -> final_auditor
                                                                                                     -> audit_report
```

统一产物状态使用 `DRAFT / FROZEN / STALE / BLOCKED / PASS`。亮点记录状态使用 `CANDIDATE / PROVEN / DROPPED`。统一证据标识使用 `Q-* / DATA-* / MODEL-* / RUN-* / VER-* / RID-* / FIG-* / TAB-* / CONTRIB-* / LAYOUT-* / ISSUE-*`。
