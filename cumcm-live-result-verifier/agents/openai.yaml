interface:
  display_name: "CUMCM 首次结果重复复核"
  short_description: "首次求解后执行复跑、独立重算与差异闭环"
  default_prompt: "Use $cumcm-live-result-verifier after the first frozen CUMCM run to repeat, independently cross-check, and gate all critical results before paper writing."

